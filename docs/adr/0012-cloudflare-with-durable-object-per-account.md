# サーバーは Cloudflare に置き、記録はアカウントごとの Durable Object に持つ

サーバーは TypeScript で書き、Cloudflare にまとめる。HTTP の受け口（Hono）は Workers、アカウントの索引（アカウント ID、Apple の識別子、セッション、Sign in with Apple のリフレッシュトークン、テストユーザー）は D1 に1つ、記録はアカウントごとに1つの Durable Object（SQLite）、写真の控えは R2 に置く。アカウントごとに DB を分けると、クエリにアカウント ID の条件を書き忘れても他人の記録が混ざらず、アカウントの削除は Durable Object を丸ごと消すことになり、1つの DB の大きさの上限（D1 は 10 GB）を気にしなくてよい。計算と DB が同じ場所で動くので、目標と目安の計算に DB との往復が要らない。週ごとの目安の見直しは、各 Durable Object のアラームで、その人のタイムゾーンで、1日の目安の適用期間（1週間）の区切りに合わせて行う。月の費用は 1,000 人で $8 程度（LLM を除く）で、D1 1つにまとめる案と同じ、Postgres を使う構成（1,000 人で月 $27〜59）の3分の1以下。

DB を東京に置くことは必須にしない。初回リリースでは「データを日本国内に置く」と約束しないため。写真と記録を送る LLM の提供元も、調べた3社のうち保存の所在地に日本を選べると確かめたのは OpenAI だけで（`docs/research/food-photo-llm.md`）、DB だけを日本に置いても国内に閉じるとは限らない。

端末に DB を同期する仕組み（Turso Sync、Embedded Replicas）は使わない。端末はふつうの API とキャッシュで記録を持つ。

## 検討した案

- D1 に全員の記録を1つにまとめる: いちばん単純だが、1つの DB が 10 GB で止まり（1人で年 5 MB と置くと約 2,000 人年）、アカウントごとの分離はクエリの条件に頼る
- Turso でアカウントごとに DB を分け、端末に同期する: 東京に置け、削除のあとに残るのは最大5日と書け、SQLite 互換なので DB を持ち出しやすい。一方、スキーマの変更を全 DB に配る仕組みが無く自前で作ることになり、Workers からの書き込みごとに最大 50 ms 待ち、Turso Database は preview で、2025 年に機能の廃止があった。オフラインで書ける同期（Turso Sync）には Swift の SDK が無く、端末が DB に直接書くと検証が端末に寄って ADR-0011 とぶつかる
- Postgres を1つ（例: 東京の Supabase を Hyperdrive でつなぐ）: いちばん持ち運びやすく、全員をまたぐ SQL も書けるが、アカウントごとの分離を捨て、固定費が月 $25 かかる

## 起きること

- Durable Objects は Cloudflare に独自の作りで、いちばん深いロックインになる。ドメイン層は Cloudflare の型に触れないふつうの TypeScript にし、スキーマは素の SQLite にする。1人の記録を読み書きするドメインの処理はその人の Durable Object の中で動かし、Durable Object のクラスは、ドメイン層を呼ぶ入口（HTTP から、アラームから）と、ドメイン層が型を定めた記録の置き場の実装だけを持つ薄い層に保つ。出口は、アカウントごとの SQLite を Turso の東京に移すこと。移る合図は、「データを日本国内に置く約束が要る」か「全員をまたぐ分析が、分析用の出来事の置き場では足りない」が本当に来たとき
- D1 と R2 は場所をアジア太平洋（`apac`）、Durable Objects は北東アジア太平洋（`apac-ne`）のヒントでしか指定できず、東京の保証はない
- 全員をまたぐ SQL は、記録に対しては書けない。推定精度の検証やユーザーの分析は、記録を書くときに分析用の出来事を別の置き場に書き出して行う
- Durable Object の中の SQL のスキーマの変更は、Cloudflare の設定では扱えない。版つきの SQL ファイルをリポジトリに置き、各 Durable Object が最初に呼ばれたときに自分の DB に当てる
- アカウントを消しても、過去の時点に戻せる履歴（期間は D1 と Durable Objects とも 30 日）に残りうる
- Workers で動かないライブラリが要る処理が出たら、その部分だけ別の基盤に置く

根拠: `docs/research/server-platform.md`、`docs/research/turso.md`、#25「サーバーの言語と実行基盤」の解決コメント
